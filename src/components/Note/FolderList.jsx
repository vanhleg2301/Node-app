/* eslint-disable react/prop-types */
/* eslint-disable no-unused-vars */
import React from "react";
import { List } from "@mui/material";

export default function FolderList({ folders }) {
  return <List>Folder list</List>;
}
